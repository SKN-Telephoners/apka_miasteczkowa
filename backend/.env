# .env file
DATABASE_URL = "postgresql://postgres:postgres@localhost/apka_miasteczkowa?sslmode=require"
TEST_DATABASE_URL = "postgresql://postgres:postgres@localhost/apka_miasteczkowa_test?sslmode=require"
JWT_SECRET_KEY = "asdsadsdasdasdasd"
MAIL_SERVER = ''  #SMTP server ex. smtp.gmail.com
MAIL_PORT = 587  #TLS or 465 for SSL
MAIL_USE_TLS = True  # if TLS True
MAIL_USE_SSL = False  # if SSL True
MAIL_USERNAME =  'your-email@example.com'
MAIL_PASSWORD = 'your-email-password'
MAIL_DEFAULT_SENDER =  'your-email@example.com'
MAIL_BACKEND = 'flask_mail.backends.locmem.EmailBackend'
TESTING = True