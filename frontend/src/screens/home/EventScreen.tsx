import React from "react";
import { View, Text } from "react-native";

const EventScreen = () => {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>Placeholder wydarzen</Text>
    </View>
    // miejsce na flatlist
  );
};

export default EventScreen;